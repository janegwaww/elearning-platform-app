import React, { Fragment, useEffect, useState } from "react";

export default function Document({ did }) {
  useEffect(() => {}, []);
  return (
    <Fragment>
      <br />
      {did}
    </Fragment>
  );
}
